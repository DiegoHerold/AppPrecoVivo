// Substituído pelo modelo unificado em '@/lib/plano-contas'.
// Mantido apenas como reexport de compatibilidade.
export {
  PlanoContaError as AccountPlanError,
  listTree as listAccountPlan,
  createGrupo,
  updateNode,
  removeNode,
  moveNode,
} from '@/lib/plano-contas'
